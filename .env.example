# ============================================================================
# Home Server Environment Variables
# ============================================================================
# 
# SECURITY WARNING:
# - This file is safe to commit (no secrets)
# - Copy to .env and fill with real values
# - NEVER commit .env file (already in .gitignore)
# - .env will be automatically chmod 600 by start.sh
#
# ============================================================================

# -----------------------------------------------------------------------------
# Timezone Configuration
# -----------------------------------------------------------------------------
# Optional: Default is Europe/Budapest
# Examples: America/New_York, Asia/Tokyo, UTC
TZ=Europe/Budapest

# -----------------------------------------------------------------------------
# Cloudflare Tunnel Token (PERMANENT SECRET)
# -----------------------------------------------------------------------------
# Get from: https://one.dash.cloudflare.com/
# Navigate to: Zero Trust > Networks > Tunnels > Create/Configure Tunnel
#
# CRITICAL SECURITY:
# - Required for cloudflared service to run
# - DO NOT share this token
# - DO NOT commit this to version control
# - Store backup in encrypted password manager
# - Rotate periodically for security
#
# Format: eyJhIjoiXXXXXX... (long base64 string)
CLOUDFLARE_TUNNEL_TOKEN=

# -----------------------------------------------------------------------------
# User/Group IDs (Optional - defaults to 1000)
# -----------------------------------------------------------------------------
# Run `id -u` and `id -g` to get your user/group IDs
# Important for file permissions on external HDD
PUID=1000
PGID=1000